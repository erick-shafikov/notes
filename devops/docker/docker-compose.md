# docker compose

утилита для работы с докер файлами, позволяет запускать несколько проектов. Работает с yaml файлами

```bash
docker-compose up # поднять docker-compose конфигурацию
docker-compose down # удалить контейнеры
docker-compose ps # процессы
```
