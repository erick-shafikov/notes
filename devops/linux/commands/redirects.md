# редиректы

```bash
free -m # размер доступной памяти
cat /dev/null > /tmp/sysinfo.txt # перенаправление вывода в файл
cat /tmp/sysinfo.txt #
freeeeeeeeee -m 2>> /tmp/error.log # перенаправления ошибок из stdout
cat /tmp/error.log #
freeeeeeeeee -m 1>> /tmp/error.log #
freeeeeeeeee -m $>> /tmp//error.log# #
sudo yum install git -y > /dev/null  # установить без логов

```
