# Свойства:

стандартизированные

## appCodeName

⇒ string "код" текущего браузера

## appName

⇒ DOMString с официальным названием браузера.

## appVersion

string;
⇒ версию браузера как DOMString.

## getBattery

BatteryManager;
⇒ BatteryManager объект, который вы можете использовать для получения информации о статусе заряда аккумулятора.

## connection

Connection;
Предоставляет Connection с информацией о сетевом подключении устройства.
NavigatorGeolocation

Geolocation;
⇒ Geolocation объект, позволяющий получить доступ к местоположению устройства.

## javaEnabled()

false;
Устарело ⇒ всегда false.

## language

DOMString | null;
⇒ предпочитаемый пользователем язык, как правило это язык пользовательского интерфейса браузера. Значение null ⇒ся, когда язык неизвестен.

## languages

DOMString[];
⇒ массив DOMString, представляющий собой языки, известные пользователю, в порядке предпочтения.

## mimeTypes

MimeTypeArray;
листинг MIME типов, поддерживаемых браузером.

## onLine

boolean;
имеет ли браузер подключение к сети.

## oscpu

string;
⇒ строку, показывающую имеющуюся операционную систему.

## platform

string;
⇒ строку, показывающую платформу браузера. Не полагайтесь на эту функцию, чтобы получить нужное значение.

## plugins

PluginArray;
⇒ PluginArray листинг плагинов, установленных в браузере.

## product

"Gecko";
Всегда ⇒ 'Gecko', в любом браузере. Это свойство сохраняется только для целей совместимости.

## userAgent

string;
⇒ строку агента пользователя для данного браузера.

## serviceWorker

ServiceWorkerContainer;
⇒ ServiceWorkerContainer объект, который обеспечивает доступ к регистрации, удалению, обновлению и связи с ServiceWorker

# Методы

стандартизированные

## getUserMedia

После запроса разрешения у пользователя ⇒ аудио или видео поток, взаимодействующий с камерой или микрофоном на локальном компьютере.

## registerProtocolHandler:

Разрешает веб-сайту зарегистрировать себя в качестве возможного обработчика для данного протокола.

## taintEnabled:

⇒ false. JavaScript taint/untaint функции отключены в JavaScript 1.2.

## vibrate:

Причина вибрации устройства с поддержкой её. Не делает ничего, если нет поддержки вибрации.
не стандартизированные

## mozIsLocallyAvailable:

Позволяет коду проверить, есть ли документ в данном URI без использования сети.

## mozPay:

Разрешает оплату в приложении.

## preference:

Устанавливает предпочтение пользователя. Этот метод доступен только для привилегированного кода и является устаревшим

вы должны использовать вместо него XPCOM Preferences API.

## requestWakeLock: ;

значок уведомлений (-ch, -ff)

## clearAppBadge(): ;

## setAppBadge(): ;;

# методы
