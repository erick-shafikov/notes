// создать функцию проверки ключа в объекте

function hasKey(obj, name) {
  // code
}
const data = { name: "Ivan" };

console.log(hasKey(data, "name")); // true
console.log(hasKey(data, "sname")); // false
