```python
from torch.nn import Embedding

emb_instance = Embedding(
    num_embeddings=1,
    embedding_dim=1,
    padding_idx=None,
    max_norm=None,
    norm_type=2.0,
    scale_grad_by_freq=False,
    sparse=False,
    _weight=None,
    _freeze=False,
    device=None,
    dtype=None,
)
```